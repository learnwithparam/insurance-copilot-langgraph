services:
  api:
    build: .
    container_name: insurance-copilot-langgraph
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/.venv
    env_file:
      - .env
    restart: unless-stopped
